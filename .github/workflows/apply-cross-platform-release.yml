name: Apply cross-platform release v1.8.0 (disabled bootstrap duplicate)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "The trusted validate-framework bootstrap owns this one-shot release application."
