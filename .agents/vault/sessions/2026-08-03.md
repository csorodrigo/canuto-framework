---
type: session
date: 2026-08-03
mode: execution
goals:
  - { text: "Avaliar se as 7 regras do print (AGENTS.md) valem para os nossos agentes", status: completed }
  - { text: "Aplicar as regras aprovadas no caminho de geração e no de patch", status: completed }
  - { text: "Corrigir o bug de crases escapadas em heredoc quoted", status: completed }
  - { text: "Rodar revisor adversarial e endereçar os strikes", status: partial }
personas-used: [Maestro, blind-reviewer]
tasks-completed: 4
provider-usage:
  claude: análise, edits, testes, commits
  blind-reviewer: 2 spawns (round 1 REQUEST CHANGES; round 2 em execução no fecho)
prs-opened: []
branch: claude/agent-feasibility-analysis-quvsma
merged: false
---

# Sessão 2026-08-03 — Triagem das 7 regras de AGENTS.md

## O que aconteceu

- Analisadas as 7 regras de engenharia propostas num print de `AGENTS.md`.
  Veredito: **3 adotadas, 2 rejeitadas por conflito direto, 1 cortada por ser
  genérica, 1 rejeitada por contradizer outra do próprio conjunto.**
- As 3 adotadas (implementação mais simples; crescer em camadas; não presumir
  que uma lib não tem a capacidade) foram aplicadas em `## Coding Rules`, nos
  **dois** caminhos de `merge_agents_md` (heredoc de geração + patcher) e no
  `AGENTS.md` da raiz.
- Corrigido bug pré-existente: `install.sh` escrevia crases escapadas dentro de
  um heredoc **quoted** (`<< 'AGENTSEOF'`), então todo projeto gerado do zero
  recebia `` \`coder\` `` literal na seção `## Codex Profiles`.
- Revisão adversarial cega (round 1): **REQUEST CHANGES**, 3 blockers e 4 minors.
  5 endereçados, 1 rejeitado, 1 corrigido de forma diferente da sugerida.
- Ao testar os cenários do revisor, apareceu um defeito que nem eu nem ele
  tínhamos visto: `grep -qF "$rule"` lia a regra como bundle de opções (toda
  regra começa com `- `). O guard falhava sempre → a segunda instalação
  duplicava as três regras. Faltava `--`.

## Decisão de escopo

Rejeitadas com motivo registrado (ver `decisions/`):

- **"Não preservar retrocompatibilidade"** — colide com
  `.agents/blind-spots/api-design.md:17`, com o gate `api-breaking`
  (`.agents/skills/governance.md:55`) e com a própria arquitetura do framework,
  que mantém `_archive/` e `.agents/memory/` legado por design. A palavra
  "fallbacks" é especialmente perigosa: o `codex-delegate.sh` tem fallback para
  Claude por contrato.
- **"Preferir libs estabelecidas"** — contradiz `AGENTS.md` "Do NOT add new
  dependencies" e "Do NOT install packages", mais `.agents/stack.md` e o gate
  `dependency-major`. Regra contraditória em arquivo sempre-carregado é pior que
  regra ausente.
- **"Componentes modulares"** — genérica, sem comportamento verificável.
- **"Sem stopgap"** — contradiz "implementação mais simples que atende ao
  requisito atual" sem regra de desempate. `architect.md:22` já roteia decisão
  arquitetural real para ADR.

## Verificação

- Suíte completa: **315 passed, 0 failed**, 2 warnings pré-existentes
  (zsh ausente; sem vault global nesta máquina).
- 6 cenários de fixture verdes: apagar uma regra (2 runs), seção ausente
  (2 runs), symlink, seção em h3, já-completo, geração fresca. Zero `.tmp` órfão.
- Teste `12f2` novo, verificado **negativamente**: perturbar qualquer uma das 3
  cópias do texto no `install.sh` faz a suíte falhar.

## Rework

`install.sh` tocado em 3 commits nesta sessão — atinge o limiar de rework de 3
do `maestro.md`. Causa identificada e não-patológica: cada commit endereça uma
camada distinta (regras → bug de crase → hardening pós-review), não é retrabalho
sobre a mesma decisão. Registrado por disciplina, sem re-plan.

## Fica aberto

- Round 2 do revisor cego não foi incorporado (rodando no fecho da sessão).
- Branch `claude/agent-feasibility-analysis-quvsma` **não mergeada**, 3 commits,
  sem PR aberto.
- Ver `pending/P-006` e `pending/P-007`.

## Links

- [[instincts/I-028-grep-dash-dash-em-string-com-hifen]]
- [[instincts/I-029-sentinel-por-item-inserido]]
- [[metrics/2026-08-03-metrics]]
