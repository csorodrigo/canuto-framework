name: Export Working Copy

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Package working copy
        run: |
          git archive --format=tar.gz --output="$RUNNER_TEMP/canuto-framework-working-copy.tar.gz" HEAD

      - name: Upload working copy
        uses: actions/upload-artifact@v4
        with:
          name: canuto-framework-working-copy
          path: ${{ runner.temp }}/canuto-framework-working-copy.tar.gz
          if-no-files-found: error
          retention-days: 1
