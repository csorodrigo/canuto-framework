---
type: session
date: 2026-03-25
mode: full
goals:
  - { text: "Fix vault: branch names aparecendo como projeto no Obsidian", status: completed }
  - { text: "Fix Obsidian Bases vazias no vault global (all-instincts, all-metrics, etc)", status: completed }
  - { text: "Popular global-instincts com instincts high-confidence", status: completed }
personas-used: [Maestro]
tasks-completed: 3
token-estimate: 0
tags:
  - session
  - vault
  - bugfix
---

# Session 2026-03-25b

## Goals

- ✅ Fix vault: branch names aparecendo como projeto (columbus, macau, monrovia, resume-ai)
- ✅ Fix Obsidian Bases vazias no vault global
- ✅ Popular global-instincts/ com instincts high-confidence

## What Was Done

**Bug 1 — Slugs de branch no lugar de projeto:**
- Diagnóstico: `project-index.json.path` revelou o mapeamento real (columbus→projeto_rh, macau→video-produtividade, monrovia→monte-sua-casa, resume-ai→resumeai-v1)
- Adicionadas 3 funções ao `install.sh`: `slug_from_conductor_path()`, `cleanup_stale_vault_slugs()`, `promote_global_instincts()` (atualizada)
- Cleanup rodou e renomeou/mergeu as 4 entradas stale automaticamente
- `cleanup_stale_vault_slugs` agora roda em todo install/update/migrate (auto-healing)

**Bug 2 — Bases vazias:**
- Causa: `file.inFolder("instincts")` busca na raiz do vault; no vault global notas estão em `projects/{slug}/instincts/`
- Fix: todos os 10 `.base` files atualizados para `file.folder.split("/").slice(-1)[0] == "X"`
- `setup_global_vault` agora sempre sobrescreve bases (era: skip se existia) para propagar fixes

**Global instincts:**
- `promote_global_instincts()` agora lê também `.agents/vault/instincts/` (vault local atomizado)
- 14 instincts high-confidence do canuto-framework promovidos para `~/.canuto/vault/global-instincts/`

**Commit:** `923870e fix(vault): auto-rename stale branch slugs and fix Obsidian Bases filters`

## Decisions Made

- Usar `project-index.json.path` (não heurística de nomes) para derivar o slug correto de entradas stale — mais confiável
- Bases sempre sobrescritas no deploy: são views auto-geradas, não customizadas pelo usuário
- `promote_global_instincts` inclui vault local do projeto atual para não depender de `--migrate` ter rodado

## Instincts Extracted

- Nenhum novo nesta sessão (I-020 a I-022 foram da sessão anterior)

## What Remains

- Obsidian Bases pode não suportar `file.folder.split()` no bloco `filters:` (só testável abrindo o Obsidian) — fallback: adicionar `type: instinct` nas templates e usar `file.hasTag()`
- `global-instincts/` tem 14 arquivos do canuto-framework; outros projetos precisam rodar `--migrate` ou `--update` para popular as suas entradas

---

*Auto-maintained by Maestro. Do not edit manually.*
